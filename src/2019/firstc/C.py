for t in range(int(input())):
  n, k = map(int, input().split())
  
  cis = list(map(int, input().split())
  dis = list(map(int, input().split())
  
  max_cis = [[K for _ in range(r)] for r in range(n)]
  max_dis = [[K for _ in range(r)] for r in range(n)]
  min_cis = [[-K for _ in range(r)] for r in range(n)]
  min_dis = [[-K for _ in range(r)] for r in range(n)]
  
  for r in range(n):
      for l in range(r):
          
      
  cols_max = [max(c) for c in bins]
  i = cols_max.index(max(cols_max))
  col = bins[i]
  j = col.index(max(col))

  print('Case #{n}: {result}'.format(n = t + 1, result=result))

